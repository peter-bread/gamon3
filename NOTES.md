# Notes

- Currently this only works for the 'github.com' host. In principle it should
  be possible to extend this to handle other hosts.

- Currently the main config file accepts a mapping of accounts to filepaths. It
  should be possible to extend this to handle other contexts.
